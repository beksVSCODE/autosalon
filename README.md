# 🚗 Автосалон - Система управления автосалоном

Полнофункциональное веб-приложение для управления автосалоном с возможностью просмотра каталога автомобилей, бронирования, авторизации пользователей и административной панели.

## 📋 Содержание

- [Технологии](#технологии)
- [Возможности](#возможности)
- [Требования](#требования)
- [Установка и запуск](#установка-и-запуск)
- [Настройка базы данных](#настройка-базы-данных)
- [Структура проекта](#структура-проекта)
- [API Endpoints](#api-endpoints)
- [Переменные окружения](#переменные-окружения)

## 🛠 Технологии

### Backend
- **Node.js** + **Express** - серверная часть
- **PostgreSQL** - база данных
- **Sequelize** - ORM для работы с БД
- **JWT** - аутентификация пользователей
- **bcrypt** - хеширование паролей
- **express-fileupload** - загрузка файлов
- **CORS** - кросс-доменные запросы

### Frontend
- **React** 17 - пользовательский интерфейс
- **React Router** - маршрутизация
- **MobX** - управление состоянием
- **React Bootstrap** - UI компоненты
- **Axios** - HTTP запросы
- **JWT Decode** - работа с токенами

## ✨ Возможности

### Для пользователей:
- 📱 Просмотр каталога автомобилей с фильтрацией по типу и бренду
- 🔍 Подробная информация о каждом автомобиле
- 📅 Бронирование автомобилей на тест-драйв или покупку
- ⭐ Добавление автомобилей в избранное
- 👤 Регистрация и авторизация
- 📋 Личный кабинет с историей бронирований
- 🔔 Просмотр статуса бронирований

### Для администраторов:
- ➕ Добавление новых автомобилей
- 🏷️ Управление брендами и типами автомобилей
- 📊 Управление бронированиями (подтверждение/отклонение)
- 👥 Просмотр всех бронирований

## 📦 Требования

- **Node.js** >= 14.x
- **PostgreSQL** >= 12.x
- **npm** или **yarn**

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <url-репозитория>
cd autosalon
```

### 2. Установка зависимостей

#### Корневая директория
```bash
npm install
```

#### Сервер
```bash
cd server
npm install
```

#### Клиент
```bash
cd client
npm install
```

### 3. Настройка переменных окружения

#### Для сервера (`server/.env`):
```env
PORT=5000
DB_NAME=auto_store
DB_USER=postgres
DB_PASSWORD=your_password
DB_HOST=localhost
DB_PORT=5432
SECRET_KEY=your_secret_key_here
```

#### Для клиента (`client/.env`):
```env
REACT_APP_API_URL=http://localhost:5000/
```

### 4. Запуск проекта

#### Запуск сервера (из папки `server`):
```bash
npm run dev
```
Сервер запустится на `http://localhost:5000`

#### Запуск клиента (из папки `client`):
```bash
npm start
```
Клиент откроется на `http://localhost:3000`

## 🗄️ Настройка базы данных

### 1. Создание базы данных PostgreSQL

```sql
CREATE DATABASE auto_store;
```

### 2. Автоматическая миграция

При первом запуске сервер автоматически создаст все необходимые таблицы благодаря Sequelize sync:

- `users` - пользователи
- `car_types` - типы автомобилей (седан, внедорожник и т.д.)
- `car_brands` - бренды автомобилей (Toyota, BMW и т.д.)
- `cars` - автомобили
- `car_features` - характеристики автомобилей
- `bookings` - бронирования
- `favorites` - избранное
- `favorite_cars` - связь избранного с автомобилями
- `type_brands` - связь типов и брендов

### 3. Создание администратора

После запуска сервера зарегистрируйтесь через интерфейс, затем вручную измените роль в БД:

```sql
UPDATE users SET role = 'ADMIN' WHERE email = 'your_email@example.com';
```

## 📁 Структура проекта

```
autosalon/
├── client/                 # React приложение
│   ├── public/            # Статические файлы
│   ├── src/
│   │   ├── components/    # React компоненты
│   │   │   ├── modals/   # Модальные окна
│   │   │   └── styles/   # CSS стили компонентов
│   │   ├── pages/        # Страницы приложения
│   │   ├── http/         # API запросы
│   │   ├── store/        # MobX store
│   │   ├── utils/        # Утилиты и константы
│   │   └── routes.js     # Маршруты
│   └── package.json
│
├── server/                # Node.js сервер
│   ├── controllers/      # Контроллеры API
│   ├── middleware/       # Middleware функции
│   ├── models/          # Модели Sequelize
│   ├── routes/          # Маршруты API
│   ├── static/          # Загруженные изображения
│   ├── error/           # Обработка ошибок
│   ├── db.js            # Подключение к БД
│   ├── index.js         # Точка входа сервера
│   └── package.json
│
└── README.md
```

## 🔌 API Endpoints

### Авторизация
- `POST /api/user/registration` - регистрация
- `POST /api/user/login` - вход
- `GET /api/user/auth` - проверка авторизации

### Автомобили
- `GET /api/device` - получить список автомобилей
- `GET /api/device/:id` - получить автомобиль по ID
- `POST /api/device` - создать автомобиль (admin)
- `DELETE /api/device/:id` - удалить автомобиль (admin)

### Типы и бренды
- `GET /api/type` - получить типы
- `POST /api/type` - создать тип (admin)
- `GET /api/brand` - получить бренды
- `POST /api/brand` - создать бренд (admin)

### Бронирования
- `POST /api/booking` - создать бронирование
- `GET /api/booking` - получить бронирования пользователя
- `GET /api/booking/admin` - все бронирования (admin)
- `PATCH /api/booking/:id/status` - изменить статус (admin)

### Избранное
- `POST /api/basket/add` - добавить в избранное
- `GET /api/basket` - получить избранное
- `DELETE /api/basket/:id` - удалить из избранного

## 🔐 Переменные окружения

### Server (.env)
| Переменная | Описание | Пример |
|------------|----------|--------|
| PORT | Порт сервера | 5000 |
| DB_NAME | Название БД | auto_store |
| DB_USER | Пользователь БД | postgres |
| DB_PASSWORD | Пароль БД | your_password |
| DB_HOST | Хост БД | localhost |
| DB_PORT | Порт БД | 5432 |
| SECRET_KEY | Секретный ключ JWT | random_secret_key123 |

### Client (.env)
| Переменная | Описание | Пример |
|------------|----------|--------|
| REACT_APP_API_URL | URL API сервера | http://localhost:5000/ |

## 👥 Роли пользователей

- **USER** - обычный пользователь (может просматривать, бронировать, добавлять в избранное)
- **ADMIN** - администратор (полный доступ ко всем функциям)

## 🐛 Устранение неполадок

### Картинки не загружаются
- Убедитесь, что сервер запущен на порту 5000
- Проверьте переменную `REACT_APP_API_URL` в `client/.env`
- Убедитесь, что папка `server/static` существует

### Ошибка подключения к БД
- Проверьте, что PostgreSQL запущен
- Убедитесь, что база данных `auto_store` создана
- Проверьте credentials в `server/.env`

### Порт уже занят
```bash
# Windows
netstat -ano | findstr :5000
taskkill /PID <PID> /F

# Linux/Mac
lsof -ti:5000 | xargs kill -9
```

## 📝 Лицензия

MIT

## 🌟 Не забудь поставить звезду!

Если проект был полезен, поставьте ⭐ на GitHub!
